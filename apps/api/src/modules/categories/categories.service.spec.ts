describe('CategoriesService', () => {
  //executa antes de todos os testes
  //beforeEach(() => {});

  //teste
  it('should sum two numbers', () => {
    const num1 = 1;
    const num2 = 2;
    const res = num1 + num2;

    expect(res).toBe(3);
  });
});
